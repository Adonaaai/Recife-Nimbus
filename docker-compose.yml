services:
  postgres:
    image: postgres:18.2
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 852123
    ports:
     - "5432:5432"
    volumes:
    - postgres:/var/lib/postgresql/data

volumes:
  postgres: